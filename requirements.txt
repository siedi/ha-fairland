colorlog==6.10.1
homeassistant==2025.12.1
# aiodns 3.5.0 (pulled in by homeassistant) breaks with pycares >= 5
# (Channel.getaddrinfo signature changed); also use Python 3.13, not 3.14.
pycares<6
pip>=26.1.2
ruff==0.14.14